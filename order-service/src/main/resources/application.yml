spring:
  datasource:
    url: jdbc:mysql://localhost:3311/order_service
    username: root
    password: ${MYSQL_ROOT_PASSWORD} # MySQL root 비밀번호
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true